# 第二天

## 1.注释

### 1.1 注释的概述

~~~

~~~

1.2 注释的分类3

~~~
1.单行注释：
以#开头，#右边的所有的数据当作文档说明，不是要真正执行的程序，起辅助说明的作用
#单行注释,以#号开头
# print("hello world")
#print("hello world")
# print("hello world")
# print("hello world")
# print("hello world")
# print("hello world")
# print("hello world")


2.#多行注释：'''或则"""开头，以'''或则"""结尾
'''
print("hello world")
print("hello world")
print("hello world")
'''
~~~

## 2.标识符和关键字

### 2.1 标识符

#### 2.1.1 标识符的定义

~~~
定义：
   开发人员在程序中自定义的一些符号和名称
   标识符是自己定义，如：变量名，函数名等
~~~

#### 2.1.2 标识符的定义规则

~~~
标识符的组成字母，下划线和数字，数字不能开头
练习：
    fromNo12    ok
    my_boolean    0k
    my-boolean     no ok
    test1         ok
    test!123      no
    G.U.I        no
    1test       no
~~~

#### 2.1.3 命名规则

~~~
1.见名知意
2.驼峰命名法
2.1 小驼峰式命名方法：第一个单词以小写字母开头，第二个单词开始每个单词的首字母大写


a = 10
# 1a = 10
# test-1= 10
# test_1 = 10
# G.U.I = 10
"""
命名规则


"""
#小驼峰
#第一个单词以小写字母开头，第二个单词开始每个单词的首字母大写
myName = 10
myname = 10
firstname = 10


#大驼峰
#每一个的满次的首字母大写
FirstName = "张三"


#3.下划线链接法
#用下划线连接所有的单词
my_name = "张三"
~~~

### 2.2 关键字

~~~
1.定义：
   python中一些具有特殊的功能的标识符----关键字
python官方 已经使用的标识符，所以不能允许开发人员自己定义和关键字同名的标识符  

2.如何查看关键字
方式1:
进入DOS---->python---->import keyword ---->keyword.kwlist
方式2：
import keyword
print(keyword.kwlist)
~~~

## 3.输入和输出

### 3.1 输出

~~~
1.python中的输出
打印提示：
print(“hello world”)

#输出
print("hello world")
print("给我的卡-----")

"""
格式化输出



"""


print("我今年10岁")
print("我今年11岁")
print("我今年12岁")
print("我今年13岁")

age = 10
print("我今年%d岁"%age)
age +=  1
print("我今年%d岁"%age)
age +=  1
print("我今年%d岁"%age)


"""
上面的例子中看到%这样的操作符，就是python中的格式化输出
"""
age = 18
name = "xiaoming"
print("我的姓名是%s,我的年龄%d"%(name,age))

#注意：使用特殊的字符d/s---->格式符号
"""
格式符号       转换
%s        通过str()字符串转换的格式化
%d        有符号的十进制整数
%c        字符
%u        无符号的十进制整数
%o        八进制
%x/X      十六进制
%f        浮点实数



"""
#5.换行输出
#在python中输出的时候，如果\n，\n后面的内容会另起一行输出

print("1234567---------")
print("1234567\n---------")

"""
练习：

================
姓名：xiaoming
qq:12345
手机号：131XXXXXXXXX
公司地址：XXXX
年龄：18
================


"""

name = "xiaoming"
qq = "1234"
address = "北京"
age = 18


print("========\n我的姓名:%s\nqq:%s\naddress:%s\nage:%d"%(name,qq,address,age))
~~~

### 3.2 输入

~~~
1.概述
2.python3：
input()
作用：获取键盘输入的数据内容input()


#输出
print("hello world")
print("给我的卡-----")

"""
格式化输出



"""


print("我今年10岁")
print("我今年11岁")
print("我今年12岁")
print("我今年13岁")

age = 10
print("我今年%d岁"%age)
age +=  1
print("我今年%d岁"%age)
age +=  1
print("我今年%d岁"%age)


"""
上面的例子中看到%这样的操作符，就是python中的格式化输出
"""
age = 18
name = "xiaoming"
print("我的姓名是%s,我的年龄%d"%(name,age))

#注意：使用特殊的字符d/s---->格式符号
"""
格式符号       转换
%s        通过str()字符串转换的格式化
%d        有符号的十进制整数
%c        字符
%u        无符号的十进制整数
%o        八进制
%x/X      十六进制
%f        浮点实数



"""
#5.换行输出
#在python中输出的时候，如果\n，\n后面的内容会另起一行输出

print("1234567---------")
print("1234567\n---------")

"""
练习：

================
姓名：xiaoming
qq:12345
手机号：131XXXXXXXXX
公司地址：XXXX
年龄：18
================


"""

name = "xiaoming"
qq = "1234"
address = "北京"
age = 18


print("========\n我的姓名:%s\nqq:%s\naddress:%s\nage:%d"%(name,qq,address,age))


"""
python3
输入：
input()
"""

height = input("请输入您的身高：")#input函数中的内容一般用于信息提示
print(type(height))

"""
说明：
input()：小括号中的内容，提示信息，用来在获取用户输入的数据之前给用户一个简单的提示
作用：
    从键盘获取数据
input():在获取键盘输入的数据后，会存放到等号左边的变量中
注意：
    input()函数会将用户输入任何形式的数据以字符串的格式进行输出

如何测试一个数据所属的数据类型
1.type()
2.isinstance()

注意：
   python2中使用raw_input()
"""
~~~

## 4.常量和变量

### 4.1 常量

~~~
定义：
   python中在程序运行的时候不会被更改的量称之为常量，
   PI = 3.1415926
   

"""
常量


"""
PI = 3.1415926
print(PI)
PI = 4
print(PI)

"""
注意：
   在定义常量的时候一般情况将常量的名字全部大写（行业规则）
特点：
   一旦绑定，不能更改
python中没有定义提供常量的保留字

"""
~~~

### 4.2 变量

#### 4.2.1 变量的概述

~~~
思考：子程序中需要对；两个数进行求和？

python中存储一个数据，需要一个叫变量的事物


"""
常量


"""
PI = 3.1415926
print(PI)
PI = 4
print(PI)

"""
注意：
   在定义常量的时候一般情况将常量的名字全部大写（行业规则）
特点：
   一旦绑定，不能更改
python中没有定义提供常量的保留字

"""

num1 = 10#num1就是一个变量，存储数据
num2 = 15#num2也是一个变量
num = num1 + num2#num:也是一个变量
print(num)
"""
变量定义：
     可以理解菜篮子，如果需要存储多个数据，最简单的方式就是定义多个变量
     变量：用来存储数据
     程序：用来处理数据
"""
#变量的定义
score = 100#定义了一个变量，名字叫score，它里面存储了一个数据 100
high = 180  #cm

applePrice = 3.5
weight = 7.5

money = applePrice * weight
money = money - 10

"""
思考，：定义一个变量应该占用多大的内存空间，保存什么样的数据？
"""
~~~

## 5.python 的数据类型

~~~
车的类型：
     卡车    轿车   拖拉机

思考：
   在程序中为什么定义数据类型：
  为了更充分的利用内存空间和以及有效的内存管理
  变量：
  有不同的数据类型
  
  在python中数据类型分为6个标准的数据类型：
  1.Number(数字)
  2.String(字符串)
  3.list(列表)
  4.元祖（tuple）
  5.集合（sets）
  6.字典（dict）
    注意：还有一个特殊的数据类型
       None
  
  如何知道一个变量的数据类型
  在python中，只要定义了一个变量，而且有值，此时它的数据类型就已经确定，不需要开发人员主动说明数据类型，系统会自动判断
     type()函数 ：
     
    
     格式：
    type(变量的名字) ：查询变量的数据类型
  
  
  
  
  
  

~~~

## 6.运算符

### 6.1 算术运算符3

~~~li&#39;ru
定义两个变量  a = 10  b = 20
运算符          描述        实例
+          加      a+b  = 30
-         减
*         乘
/         除
//       取整     9//2 ----》4    9.0//2.0-----》4.0
%        取余    返回除法的余数    b%a   ---》 0    3%2  ----》  1
**       幂      返回a的b次幂    a ** b    
例如：
9/2.0 ---->4.5
~~~

### 6.2 赋值运算符

~~~
运算符      描述      实例
=      赋值运算符    把=右边的结果给左边的变量   num = 1+2*3   num = 7

赋值运算表达式：
   格式：
      变量 = 表达式
功能：计算了等号右边的表达死的结果，将其结果赋值给等号左边的变量
值：赋值后变量的结果

num1 = 20
num2 = num1 + 20
print(num2)
~~~

### 6.3 复合运算符

~~~
+=    加法赋值运算符    c += a   等效于   c = c+a
-=    减法赋值运算符    c -= a   ---->    c = c-a
*=    乘法赋值运算符    c *= a  ---->    c = c*a
/=    除法赋值运算符    c /= a  ---->   c = c/a
%=    取余赋值运算都    c %= a  ----->  c = c%a
**=   幂赋值运算符      c **= a ----->   c = c**a
//=   取整赋值运算符    c //= a ----->   c = c // a
~~~

### 6.4 比较运算符

~~~
运算符        描述      实例
==       检查两个操作的值是否相对   如果条件为正，返回true
!=       检查两个操作数的值是否相等   如果条件为正，，返回False
>       检查左边的操作数的值是否大于右边的操作数的值   如果条件为正    返回true
<       检查左边的操作数的值是否小于右边的操作数的值   如果条件为正    返回true
>=     检查左边的操作数的值是否大于等于右边的操作数的值   如果条件为正    返回true
<=     检查左边的操作数的值是否小于等于右边的操作数的值   如果条件为正    返回true


注意：
   使用比较运算符，得到的结果只有两个，True，False，一般用于判断
~~~

### 6.5 逻辑运算符

~~~
运算符       逻辑表达式       描述        实例
and        x and y 布尔与   如果x为False   x and y  返回False  否则返回y的计算结果

案例：#and
a = False
b = 20
print(a and b)
a = 1
b = 3
c = a > b
print( c and b)


or      x or y  布尔或   如果x是True，返回True，反之返回y的计算值

not   not x布尔非   如果x为True  返回False   反之   x为False   结果为True

~~~

### 6.6 位运算符

~~~
概述：
    按位运算符：把数字看作二进制来进行计算
    
   python中安慰哦运算符的规则
   a = 60    b = 13
   60----> 0011 1100
   13----> 0000 1101
   
 运算符         描述       实例
 &    按位与运算符：参与运算的两个值，如果两个相应的位都为1，该位的结果就为1，反之为0
 a & b       0000 1100   ----12  
   
 |     按位或运算符：如果对应的两个二进制位有一个为1，此时结果位为1
 a | b   0011 1101     ----->61
   
 ^     按位异或运算符：当两个对应的二进制位相异时，结果为1
 a ^ b   0011 0001    ------>49
   
~      按位取反运算符 ：对数据的每一个二进制位进行取反，取反的结果1变0，0变1
~a     ------>    -61

<<   左移运算符：运算数的二进制全部的左移若干位，移动的位置的大小由<<右边的数字来确定，高位丢弃，低位补0
a << 2
0011 1100
1111 0000------240

>> 右移运算符：把>>iang左边的运算数各二进制全部右移若干位，>>右边的数字来确定,高位补0，低位舍弃
 a >> 2
 000 1111----->15

a >> 3
----->7

思考：
如何以最快的方式表示2^3
~~~

## 7.条件控制语句

### 7.1 if条件语句概述

~~~
应用的场景
生活中：进火车站
开发：用户登陆应用，密码的判断

例如：
    重要的日子5-20
    if 今天是情人节：
    	买玫瑰
    
    
    if 今天发工资：
    	先还信用卡
    	if 有剩余：
    	     可以happy
    	
        
    总结：
       如果某些条件满足，才能做某件事情，当不满足条件是不能做------》判断
 
~~~

### 7.2 if判断语句的格式

#### 7.2.1 if语句的基本格式

~~~
if语句用来判断

格式：
if 要判断的条件:
	条件成立，执行事情
age = 24
if age >= 25:
    print("可以领证....")

注意：
如果条件不成立，if判断的语句体的内容不执行
作用：
    当满足一定的条件才会执行那段代码，否则不执行

练习：
从键盘获取自己的年龄，判断是否大于18岁，如果大于十八岁，可以做......
input()--->string

age = input("请输入您的年龄：")
age = int(age)
if age >= 18:
    print("哥已成年，可以happy。。。。。")

思考：
   判断age大于等于18岁，>=    还可以用那些？
~~~

### 7.2.2  if...else

~~~~
在使用if语句时只能满足条件成立时，做的事情，，那么万一需要在条件不满足的情况下执行某件事
-----》else

格式：
if 条件：
    满足条件要做的事情1
    满足条件要做的事情2
    满足条件要做的事情3
    ....
else:
	不满足条件要做的事情1
    不满足条件要做的事情2
    不满足条件要做的事情3
    ....

chepiao = 1 #1:代表有票，0：表示没票
if chepiao == 1:
    print("有车票....可以上火车.....")
    print("可以见到她，乐呵呵，美滋滋.....")
else:
    print("亲爱的，下次再见....")


练习：
要求，从键盘输入刀子的长度，如果刀子的长度没有超过10cm,允许带上火车，反之不允许



"""
从键盘输入刀子的长度，
如果刀子的长度没有超过10cm,允许带上火车，反之不允许

"""
# knifeLenght = int(input("请输入刀子的长度："))
# if knifeLenght >=10:
#     print("不允许进入")
# else:
#     print("允许")

#if比较运算符

# age = 19
# if age >= 18:
#     print("成年，去网吧.....")
#
#案例2：if逻辑运算符-or
you = input("你去吗？")#去或者不去
yourWife = input("你去吗？")  #去或者不去
#if you  或者  yourwife
if you == "去" or yourWife == "去":
    print("可以成功的办好某件事....")
#案例3：逻辑and
if you == "去" and yourWife == "去":
    print("可以成功的领证....")

#案例4：找对象，白富美

color = input("你白吗？")
money = input("你富吗？")
beautiful= input("你美吗？")
#if 白 并且 富 并且 美
#if 白 and 富 and 美
if color == "白" and money > 10000000 and beautiful == "美":
    print("晚上可以一起吃饭....")

else:
    print("......")

#案例：if逻辑运算符--not
a = 30
if not a > 0 and a <= 50:
    pass

~~~~

### 7.2.3 elif

~~~
格式：
    if xxx1:
         事情1
    elif xxx2:
         事情2
    elif xxx3:
         事情3
       ....
       
说明：
    当XXX1满足时，执行事情1，此时整个if语句结束
    当xxx1不满足，判断xxx2<如果条件2满足，执行事情2，然后整个if语句结束
    当xxx1不满足，xxx2不满足，如果xxx3满足，执行事情3，然后整个if语句结束
    .......
    
 案例：
 根据考试的成绩判断等级(键盘录入)
 90~100   A
 80~90    B
 70~80    C
 
 
"""
 根据考试的成绩判断等级(键盘录入)
 90~100   A
 80~90    B
 70~80    C
"""
score = int(input("请输入学生的成绩："))

if score >= 90 and score <= 100:
    print("A")
elif score >= 80 and score < 90:
    print("B")
elif score >= 70 and score < 80:
    print("C")
elif score >= 60 and score < 70:
    print("D")
elif score <= 60 and score >= 0:
    print("E")


#案例：
sex = input("请输入您的性别：")
if sex == "男":
    print("你是男性，可以留胡子")
elif sex == "女":
    print("你是女性。可以穿裙子")
else:
    print(".....")
# elif sex == "other":
#     print("。。。。。")
"""    
说明：
当性别为男性，条件满足，输出男性的特征...
判断elif中条件，条件满足，执行elif中的语句体
当所有的条件不满足，执行else中的语句体


elif的注意点    
    elif可以和else一起使用
    
 elif和else必须和if一起使用，出错   
    
    
"""
"""
根据用户的数据输入判断今天是星期几？

"""
num = int(input("请输入一个数字（1~7）"))

if num == 1:
    print("星期一")
elif num == 2:
    print("星期二")
elif num == 3:
    print("星期三")
elif num == 4:
    print("星期四")
elif num == 5:
    print("星期五")
elif num == 6:
    print("星期六")
elif num == 7:
    print("星期日")
else:
    print("输入有误")
~~~

### 7.3 if语句的嵌套

### 7.3.1 概述

~~~

~~~

### 7.3.2 if嵌套的格式

~~~
格式：
   if 条件1:
       满足条件1，做事情1
       满足条件1，做事情2
       满足条件1，做事情3
       ....
       if 条件2：
       	    满足条件2，做事情1
       	    满足条件2，做事情2
       	    满足条件2，做事情3
       	    .....
说明：
    外层的if判断，也可以写if..else
    内层的if判断，也可以写if..else

例如：火车进站


"""
火车安检
"""
ticket = 1 #1:表示有车票，0:表示没有车票
knifeLength = 9#cm
#先判断是否有票
if ticket == 1:
    print("通过了车票的安检，进入车站，等待安检...")
    if knifeLength <= 10:
        print("通过安检，进入候车大厅...")
    else:
        print("有点长，等待警察叔叔的问候.....")
else:
    print("兄弟，你还没有票....")

~~~

### 7.4 if语句的应用（猜拳游戏）

~~~
简单的分析：
   电脑随机生成一个正整数（0~2）分别入下：
   剪刀（0）   石头（1）  布（2）：
   要求用户同过键盘的方式输入0/1/2进行比较，得出结果
   赢了    输了   平局


随机数：
import random
num = random.randint(0,2)
print(num)












~~~





































 